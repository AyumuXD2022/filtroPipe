import { FilterPersonaPipe } from './filter-persona.pipe';

describe('FilterPersonaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPersonaPipe();
    expect(pipe).toBeTruthy();
  });
});
